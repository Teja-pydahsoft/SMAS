HOST=0.0.0.0
PORT=8000

# CPU-only setup (no GPU required)
INSIGHTFACE_MODEL=buffalo_l
CPU_THREADS=4
DET_SIZE=640,640

# Use buffalo_s + DET_SIZE=320,320 if gate scans feel slow on older CPUs

FACE_MATCH_THRESHOLD=0.42
MIN_MATCH_MARGIN=0.05
SEARCH_TOP_K=5
INDEX_DIR=data
