# ============================================================
# SMAS app instance — Caddy / compose settings
# Copy to .env — backend settings live in backend.env instead.
# ============================================================

# The public hostname of the app (A record → this instance's static IP).
# Caddy uses this to obtain the HTTPS certificate.
# No domain yet? Use sslip.io with your static IP: 13-234-119-62.sslip.io
DOMAIN=smas.example.com
